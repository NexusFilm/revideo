{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm install --prefix apps/nexia-motion && npm run build --prefix apps/nexia-motion",
  "outputDirectory": "apps/nexia-motion/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
